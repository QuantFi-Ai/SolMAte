# Network timeout for production builds
network-timeout 300000

# Disable cache to ensure fresh builds
cache-folder /tmp/yarn-cache

# Force fresh installs
prefer-offline false

# Disable progress bar for CI
progress false

# Disable emoji
disable-emoji true